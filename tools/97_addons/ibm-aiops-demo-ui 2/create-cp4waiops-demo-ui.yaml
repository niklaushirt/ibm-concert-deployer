---
apiVersion: v1                     
kind: Namespace
metadata:
  name: ibm-demo-ui
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ibm-demo-ui
  namespace: ibm-demo-ui
  labels:
      app: ibm-demo-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ibm-demo-ui
  template:
    metadata:
      labels:
        app: ibm-demo-ui
    spec:
      containers:
      - image: quay.io/niklaushirt/ibm-aiops-demo-ui:5.1.2
        imagePullPolicy: Always
        name: ibm-demo-ui
        command:
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "50m"
            memory: "50Mi"
          limits:
            cpu: "550m"
            memory: "550Mi"
          readinessProbe:                 
            failureThreshold: 9           
            httpGet:                      
              path: /health          
              port: 8000                  
              scheme: HTTP           
            initialDelaySeconds: 60       
            periodSeconds: 10             
            successThreshold: 1           
            timeoutSeconds: 3
        envFrom:
        - configMapRef:
            name:  ibm-demo-ui-config
        - configMapRef:
            name:  ibm-demo-ui-config-events
        - configMapRef:
            name:  ibm-demo-ui-config-logs
        - configMapRef:
            name:  ibm-demo-ui-config-metrics
      serviceAccount: ibm-demo-ui-admin
---
apiVersion: v1
kind: Service
metadata:
  name: ibm-demo-ui-service
  namespace: ibm-demo-ui
spec:
  selector:
    app: ibm-demo-ui
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name:  ibm-demo-ui
  namespace: ibm-demo-ui
spec:
  to:
    kind: Service
    name: ibm-demo-ui-service
    weight: 100
  port:
    targetPort: 8000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
  wildcardPolicy: None
